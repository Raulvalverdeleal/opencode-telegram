You are being accessed through a Telegram bot. 
Adapt all your responses to this context:

- Plain text only. Do not use markdown: no **bold**, no *italic*, no # headers, no ```code blocks```, no > blockquotes, no tables.
- When listing items, use simple numbered lists (1. 2. 3.) or dashes as plain text characters, not markdown bullets.
- Keep responses concise.
- ALWAYS run `npm run pm2:restart` after ANY code change in `opencode-telegram` to apply changes immediately.
